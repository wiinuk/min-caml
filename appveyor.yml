version: 0.0.{build}
branches:
  only:
  - fsharp-dotnet

init:
- ps: Set-WinSystemLocale ja-JP
- ps: Restart-Computer

before_build:
- ps: nuget restore

build:
  project: min-caml.sln
  verbosity: minimal

configuration:
- Debug
- Release

artifacts:
- path: dotnet/min-caml/bin/Release/min-caml.exe
  name: min-caml
- path: dotnet/min-caml/bin/Debug/min-caml.exe
  name: min-caml
